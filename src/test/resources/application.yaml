spring:
  main:
    banner-mode: off
app:
  name: aareg-teknisk-historikk-test
app.aareg.services:
  url: '${wiremock.server.baseUrl}'
azure.app:
  wellKnownUrl: '${wiremock.server.baseUrl}/wellknown'
server:
  port: 23456
management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: prometheus,health
  metrics:
    export:
      prometheus:
        enabled: true