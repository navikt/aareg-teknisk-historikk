app:
  name: aareg-teknisk-historikk-test
  url.aareg.services: http://localhost:${wiremock.server.port}/aareg-services
  url.aareg.tilgangskontroll: http://localhost:${wiremock.server.port}/aareg-tilgangskontroll
  texas.target.aareg.services: "test:arbeidsforhold:aareg-services"
  texas.target.aareg.tilgangskontroll: "test:arbeidsforhold:aareg-tilgangskontroll"
  security:
    oidc:
      idp:
        maskinporten:
          issuerUrl: http://localhost:${wiremock.server.port}/maskinporten
          jwkSetUri: http://localhost:${wiremock.server.port}/maskinporten/jwks

nais.token.exchange.endpoint: http://localhost:${wiremock.server.port}/exchange
nais.token.endpoint: http://localhost:${wiremock.server.port}/token
